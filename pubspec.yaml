name: ios_preflight
description: A Flutter dev-dependency CLI that validates a project's iOS submission readiness before you build or open Xcode.
version: 1.2.0
repository: https://github.com/ITSNAYAN/dartPackage_ios_preflight
issue_tracker: https://github.com/ITSNAYAN/dartPackage_ios_preflight/issues

topics:
  - ios
  - flutter
  - cli
  - preflight
  - lint

environment:
  sdk: ^3.11.0

executables:
  ios_preflight:

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
